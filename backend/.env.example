# backend/.env.example
# Copy → backend/.env and fill in real values. Never commit .env.

GEMINI_API_KEY=your_gemini_api_key_here

# Path inside the container — data/ is mounted at /app/data, keep as-is.
VECTOR_DB_PATH=/app/data/vectorstore/chroma.sqlite3

TOP_K=10
